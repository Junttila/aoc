const solutions: Array<(lines: string[]) => string> = [
  // Solution part 1
  (lines: string[]) => {
    lines.length++;
    return 'no answer';
  },
  // Solution part 2
  (lines: string[]) => {
    lines.length++;
    return 'no answer';
  },
];

export default solutions;
